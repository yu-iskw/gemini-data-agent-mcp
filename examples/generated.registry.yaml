# Example output shape from admin tool `generate_analyst_registry_yaml`.
# Commit through normal Git review; analysts consume this as their static registry file.

api_version: v1beta

agents:
  my-agent:
    data_agent: projects/my-gcp-project/locations/us-central1/dataAgents/my-agent
    tools:
      - chat_data_agent
      - create_data_agent_conversation
      - list_conversation_messages
      - query_data_agent
