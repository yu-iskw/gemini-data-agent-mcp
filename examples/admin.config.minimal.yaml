# yaml-language-server: $schema=../schemas/app-config.v2.schema.json
# Minimal admin MCP config (same v2 schema as analyst registry).

api_version: v1beta

server:
  name: gemini-data-agent-admin-mcp
  log_level: INFO
  transport: stdio

agents:
  my-agent:
    data_agent: projects/my-gcp-project/locations/us-central1/dataAgents/my-agent
    tools:
      - query_data_agent
